## Summary

<!-- What does this PR change and why? Link the issue it closes (Closes #N) if any. -->

## Changes

-

## Testing

<!-- How was this verified? `pytest` output, manual checks, etc. -->

- [ ] `ruff check llm_cache_router/ tests/` passes
- [ ] `ruff format --check llm_cache_router/ tests/` passes
- [ ] `mypy llm_cache_router/ --ignore-missing-imports` passes
- [ ] `pytest` passes, new behaviour is covered by tests

## Notes for reviewers

<!-- Anything non-obvious: backwards compatibility, breaking changes, rollout steps. -->
